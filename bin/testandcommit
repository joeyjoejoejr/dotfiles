#! /bin/bash

bundle exec rake &&
git commit -am "$*"

if [[ $? == 0 ]]; then
  /usr/local/bin/tmux display-message "commit sucessfull"
else
  /usr/local/bin/tmux display-message "commit exited with status $?"
fi
